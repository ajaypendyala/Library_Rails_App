<h1> You Shall look at a Form Soon!</h1>

<%= form_for(@booking)  do |f| %>
    <%= f.label :Room %>
    <%= f.collection_select(:Room_id, Room.all, :id, :to_s) %>
    

            
            

            <div class="field">
              <%= f.label :start_time %><br>
              <%= f.datetime_local_field :start_time %>
            </div>
    
            <div class="actions">
               <%= f.submit %>
            </div>
         
<% end %>